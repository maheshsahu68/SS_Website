# SonicSearch Client

React + Vite frontend for SonicSearch.

## Run locally

```bash
npm install
npm run dev
```

App URL: `http://localhost:5173`

> The frontend expects backend API at `http://localhost:5000`.

## Quality checks

```bash
npm run lint
npm run build
```
